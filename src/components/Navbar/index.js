import React from 'react';
import './styles.css';

const Navbar = () => (
        <nav id="nav-bar">
            <img alt="logo" src={ require('../../img/main/dc_logo.png') }/>
            <p> DC UNIVERSE CHARACTERS </p>
        </nav>
);

export default Navbar;
